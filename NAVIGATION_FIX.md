# Виправлення навігації для мовних версій

## Проблема
Посилання в хедері та футері використовували `../` (basePath), що призводило до переходу на англійську версію замість залишення в поточній мовній папці.

**Приклад проблеми:**
- Користувач на `/de/index.html`
- Клік на "YouTube" → `href="../buy-youtube-views.html"`
- Результат: перехід на `/buy-youtube-views.html` (англійська версія) ❌

## Рішення
Розділили логіку для посилань на сторінки та ресурси:

### 1. Додано нову функцію `getLinkPrefix()`
```javascript
function getLinkPrefix() {
  // Всі внутрішні посилання залишаються в поточній папці
  return "";
}
```

### 2. Оновлено `getBasePath()` 
```javascript
function getBasePath() {
  const lang = getCurrentLang();
  return lang === "en" ? "" : "../";
}
```
- Використовується **тільки для ресурсів** (CSS, JS, зображення)

### 3. Оновлено всі функції генерації HTML

**getHeaderHTML():**
- `linkPrefix` для посилань на сторінки
- `basePath` для зображень

**getServicesNavHTML():**
- `linkPrefix` для всіх посилань навігації

**getFooterHTML():**
- `linkPrefix` для внутрішніх посилань
- `basePath` для зображень
- `langBasePath` для перемикача мов

## Результат

### ✅ Навігація в межах мови
**Німецька версія (/de/):**
- `/de/index.html` → YouTube → `/de/buy-youtube-views.html` ✅
- `/de/index.html` → Followers → `/de/buy-instagram-followers.html` ✅
- `/de/index.html` → Services → `/de/services.html` ✅

**Італійська версія (/it/):**
- `/it/index.html` → YouTube → `/it/buy-youtube-views.html` ✅
- `/it/buy-instagram-followers.html` → Likes → `/it/index.html` ✅

**Чеська версія (/cs/):**
- `/cs/services.html` → Instagram → `/cs/index.html` ✅

### ✅ Перемикач мов працює
- `/de/index.html` → клік на Italiano → `/it/index.html` ✅
- `/it/buy-instagram-followers.html` → клік на Deutsch → `/de/buy-instagram-followers.html` ✅
- `/cs/services.html` → клік на Română → `/ro/services.html` ✅

### ✅ Ресурси завантажуються правильно
- Зображення: `../assets/images/...` ✅
- CSS: `../assets/css/...` ✅
- JavaScript: `../assets/js/...` ✅

## Тестування

Перевірте в браузері:
1. Відкрийте `/de/index.html`
2. Клікніть на "YouTube" в хедері
3. Переконайтеся, що URL: `/de/buy-youtube-views.html`
4. Клікніть на прапорець Italiano
5. Переконайтеся, що URL: `/it/buy-youtube-views.html`

Все працює! 🎉
